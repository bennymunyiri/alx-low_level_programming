#include <stdio.h>
/**
 * main - Entry point
 * Description: Of base 10
 * Return: always '0' if succesful
 */
int main(void)
{
	int n;

	for (int n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
