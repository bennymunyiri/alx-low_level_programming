#include <stdio.h>
/**
 * main - Entry point
 * Description: Of base ten
 * Return: always zero if succesful
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
