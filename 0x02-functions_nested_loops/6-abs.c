#include <main.h>
/**
 * main - entry point
 * Description: IS the number positive or negative
 * Return: the number if positive or negative
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
